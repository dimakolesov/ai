meta:
  environment: python
  toolchain:
    name: pip
    version: 3.8
build:
  requirementsPath: bot/requirements.txt
run:
  scriptName: bot/main.py
  persistenceMount: bot/logs
  containerPort: 80
